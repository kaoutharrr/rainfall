#include <stdio.h>
#include <stdlib.h>

int m = 0;  // global at 0x8049810

void p(char *buf) {
    printf(buf);              // format string vulnerability
}

void n() {
    char buf[512];

    fgets(buf, 512, stdin);
    p(buf);

    if (m == 0x1025544) {
        system("/bin/sh");
    }
}

int main() {
    n();
    return 0;
}